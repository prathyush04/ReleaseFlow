version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    container_name: releaseflow-postgres
    environment:
      POSTGRES_DB: releaseflow
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: releaseflow-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
